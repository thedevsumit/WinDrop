name: Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-cpp:
    name: Build C++ core/sender (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Set up MSYS2 (Windows only)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-gcc

      - name: Build core + sender (Linux)
        if: runner.os == 'Linux'
        working-directory: backend
        run: |
          g++ -pthread -std=c++17 core.cpp sha256.cpp net_platform_posix.cpp -o core
          g++ -std=c++17 sender.cpp sha256.cpp net_platform_posix.cpp -o sender

      - name: Build core + sender (Windows)
        if: runner.os == 'Windows'
        working-directory: backend
        run: |
          g++ -pthread -std=c++17 core.cpp sha256.cpp net_platform_win.cpp -o core.exe -lws2_32 -liphlpapi
          g++ -std=c++17 sender.cpp sha256.cpp net_platform_win.cpp -o sender.exe -lws2_32 -liphlpapi
        shell: bash

  unit-tests:
    name: Run unit tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Set up MSYS2 (Windows only)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-gcc

      - name: Build and run SHA-256 tests (Linux)
        if: runner.os == 'Linux'
        working-directory: tests
        run: |
          g++ -std=c++17 -I../backend test_sha256.cpp ../backend/sha256.cpp -o test_sha256
          ./test_sha256

      - name: Build and run SHA-256 tests (Windows)
        if: runner.os == 'Windows'
        working-directory: tests
        run: |
          g++ -std=c++17 -I../backend test_sha256.cpp ../backend/sha256.cpp -o test_sha256.exe
          ./test_sha256.exe
        shell: bash

      - name: Build and run metadata/resume tests (Linux)
        if: runner.os == 'Linux'
        working-directory: tests
        run: |
          g++ -std=c++17 -I../backend test_metadata.cpp -o test_metadata
          ./test_metadata

      - name: Build and run metadata/resume tests (Windows)
        if: runner.os == 'Windows'
        working-directory: tests
        run: |
          g++ -std=c++17 -I../backend test_metadata.cpp -o test_metadata.exe
          ./test_metadata.exe
        shell: bash

  node-syntax:
    name: Backend JS syntax check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check index.js syntax
        working-directory: backend
        run: node --check index.js